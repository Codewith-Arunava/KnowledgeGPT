# Frontend environment variables
# Copy to .env.local for local overrides (not committed)

# Leave empty for local dev (uses Vite proxy → localhost:8000)
# Set to your deployed API URL in production (e.g. https://api.your-domain.com)
VITE_API_URL=
